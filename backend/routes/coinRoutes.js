const express = require('express');\nconst router = express.Router();\nconst { protect } = require('../middleware/auth');\nconst {\n  getBalance,\n  getPackages,\n  createTopup,\n  verifyTopup,\n  spendCoins,\n  getTransactionHistory,\n  addBonusCoins\n} = require('../controllers/coinController');\n\n// Get coin balance\nrouter.get('/balance', protect, getBalance);\n\n// Get all coin packages\nrouter.get('/packages', getPackages);\n\n// Create top-up checkout\nrouter.post('/topup', protect, createTopup);\n\n// Verify top-up after payment\nrouter.post('/topup/verify', protect, verifyTopup);\n\n// Spend coins\nrouter.post('/spend', protect, spendCoins);\n\n// Get transaction history\nrouter.get('/transactions', protect, getTransactionHistory);\n\n// Add bonus coins (admin only)\nrouter.post('/bonus', protect, addBonusCoins);\n\nmodule.exports = router;\n